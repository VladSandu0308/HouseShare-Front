name: Docker Image CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag front
      
  deploy:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Load In Swarm
      run: curl -XPOST http://192.168.1.126:9000/api/webhooks/5c17a1d8-f5e2-4c14-85d7-ec7849bce3da
    
